# Dependencies are resolved exclusively through pnpm. Do not commit other lockfiles.
engine-strict=true
strict-peer-dependencies=false
# Workspace packages resolve from source during development, never from the registry.
link-workspace-packages=true
prefer-workspace-packages=true
